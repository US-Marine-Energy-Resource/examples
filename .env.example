# Only needed for the NLR API backend (large wave-hindcast pulls); small queries
# read from S3 anonymously. us-marine-energy-resource reads these from the
# environment, ./.env, or ~/.mer.env (works from any directory).
NLR_DEVELOPER_EMAIL=
NLR_DEVELOPER_API_KEY=
